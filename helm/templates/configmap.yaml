apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "request-tracker.configName" . }}
  labels:
    {{- include "request-tracker.labels" . | nindent 4 }}
data:
  rtSiteConfig: |-
{{ .Values.rtConfig.rtSiteConfig | indent 4 }}
  caddyfile: |-
{{ .Values.config.caddyfile | indent 4 }}
