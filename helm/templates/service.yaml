apiVersion: v1
kind: Service
metadata:
  name: {{ include "request-tracker.fullname" . }}
  labels: {{ include "request-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{ include "request-tracker.selectorLabels" . | nindent 4 }}
